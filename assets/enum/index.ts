export enum PLAYER_STATE{
    IDLE = 'IDLE',
    walk = 'WALK',
    DEAD = 'DEAD',
}

export enum PLAYER_DIR{
    TOP = 'TOP',
    LEFT = 'LEFT',
    RIGHT = 'RIGHT',
    BOTTOM = 'BOTTOM',
}

export enum EVENT_TYPE{
    STATE_CHANGE = 'STATE_CHANGE'
}